import Foundation

/// Current build version. Increment minor version for each release commit.
/// Format: MAJOR.MINOR.PATCH (e.g. 1.0.67, 1.0.2)
enum AppVersion {
    static let current = "1.0.67"
}
